machine:
  python:
    version: 2.7.10

dependencies:
  pre:
    - make settings
  post:
    - pip install pylint

test:
  override:
    - pylint buildservice --rcfile=pylint.rc
    - ./manage.py test
